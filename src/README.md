# Source Code Note

The main project scripts currently live in the repository root so existing local paths continue to work.

Core scripts:

- `build_sp500_factor_database.py`
- `run_single_factor_tests.py`
- `factor_detector_v2.py`
- `validate_portfolio_rules.py`
- `run_locked_final_test.py`

If this project is later refactored into a package, these files can be moved into `src/` and imports can be updated accordingly.

